const jwt = require("jsonwebtoken");
var sql = require("mssql");
require('dotenv').config();


const sqlConfig = {
    user: "wwe",
    password: "wwe",
    server: "OS",
    database: "E-shamDB",
    port: 1433
}

class ContentActivityController {

    
    add = async (req, res) => {
        const typeID = req.query.typeID;
        const activityDate = new Date().toISOString().slice(0, 10);
        const description = req.query.description;
        const contentID = req.query.contentID;
        if (apikey === process.env.APIKEY) {


            sql.connect(sqlConfig, function (err) {

                if (err) res.send(err);
                // create Request object
                var request = new sql.Request();
                
                const qr = 'INSERT INTO contentActivity (typeID,activityDate,description,contentID)'+
                ' VALUES  (N\'' + typeID + '\',N\'' + activityDate + '\',N\'' + description + '\',N\'' + contentID + '\')';
         
                // query to the database and get the records
                request.query(qr, function (err) {
                    if (err) {
                        res.send(err);
                    }

                    else {
                       
                        res.send("the category add successfully");
                    }
                });
            });
        }
        else {
            res.send("Invalid Key");
        }

    }

   
    

    read = async (req, res) => {
        
        const apikey = req.query.ApiKey;
        if (apikey === process.env.APIKEY) {


            sql.connect(sqlConfig, function (err) {

                if (err) res.send(err);
                // create Request object
                var request = new sql.Request();
                
                const qr = "select *  from contentActivity";
           
                // query to the database and get the records
                request.query(qr, function (err , recordset) {
                    if (err) {
                        res.send(err);
                    }

                    else {
                       
                        res.send(recordset.recordset);
                    }
                });
            });
        }
        else {
            res.send("Invalid Key");
        }

    }
}

module.exports = new ContentActivityController();
